title: "PortusSophia™"
description: "The public research interface for PortusSophia™, an independent programme and candidate field architecture for disciplined cross-field inquiry."
url: "https://public.portussophia.com"
baseurl: ""
asset_version: "20260717-5"

# The site owns its layouts, includes, and styles. No remote or packaged theme
# supplies hidden header, footer, navigation, or page-shell behavior.
markdown: kramdown
permalink: pretty

defaults:
  - scope:
      path: ""
    values:
      header_surface: light
  - scope:
      path: "index.md"
    values:
      header_surface: dark
sass:
  style: compressed

plugins:
  - jekyll-seo-tag
  - jekyll-feed

github:
  is_project_page: false

exclude:
  - _work
  - archive
  - README.md
  - LICENSE.md
  - Gemfile
  - Gemfile.lock
